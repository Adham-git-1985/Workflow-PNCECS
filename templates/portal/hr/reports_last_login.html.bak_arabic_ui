{% extends "portal/hr/base.html" %}
{% block title %}تقرير آخر دخول ناجح{% endblock %}

{% block hr_content %}
<div class="p-4 bg-white rounded-3 shadow-sm mb-3">
  <div class="d-flex align-items-center justify-content-between flex-wrap gap-2">
    <div>
      <h4 class="mb-1"><i class="bi bi-shield-check"></i> تقرير آخر دخول ناجح الموظفين إلى النظام</h4>
      <div class="text-muted">فلترة حسب الموظف/موقع العمل/نطاق التاريخ.</div>
    </div>
    <div class="d-flex gap-2">
      <a class="btn btn-outline-secondary" href="{{ url_for('portal.hr_reports_home') }}"><i class="bi bi-arrow-right"></i> رجوع</a>
      {% if can_export %}
        <a class="btn btn-outline-success" href="{{ url_for('portal.hr_report_last_login', user_id=selected_user_id, work_location_id=selected_work_location_id, from_date=from_date, to_date=to_date, export='xlsx') }}">
          <i class="bi bi-file-earmark-excel"></i> Excel
        </a>
      {% endif %}
    </div>
  </div>
</div>

<div class="bg-white rounded-3 shadow-sm p-3 mb-3">
  <form method="get" class="row g-2 align-items-end">
    <div class="col-md-4">
      <label class="form-label">الموظف</label>
      <select class="form-select" name="user_id">
        <option value="">الكل</option>
        {% for u in users %}
          <option value="{{ u.id }}" {% if selected_user_id==u.id %}selected{% endif %}>{{ u.full_name or u.name or u.email }}{% if u.email %} — {{ u.email }}{% endif %}</option>
        {% endfor %}
      </select>
    </div>
    <div class="col-md-3">
      <label class="form-label">موقع العمل</label>
      <select class="form-select" name="work_location_id">
        <option value="">الكل</option>
        {% for x in work_locations %}
          <option value="{{ x.id }}" {% if selected_work_location_id==x.id %}selected{% endif %}>{{ x.name }}</option>
        {% endfor %}
      </select>
    </div>
    <div class="col-md-2">
      <label class="form-label">من تاريخ</label>
      <input class="form-control" type="date" name="from_date" value="{{ from_date }}">
    </div>
    <div class="col-md-2">
      <label class="form-label">إلى تاريخ</label>
      <input class="form-control" type="date" name="to_date" value="{{ to_date }}">
    </div>
    <div class="col-md-1">
      <button class="btn btn-primary w-100" type="submit"><i class="bi bi-search"></i></button>
    </div>
  </form>
</div>

<div class="bg-white rounded-3 shadow-sm p-3">
  <div class="text-muted small mb-2">عدد النتائج: {{ rows|length }}</div>
  <div class="table-responsive">
    <table class="table table-sm align-middle">
      <thead>
        <tr>
          <th>#</th>
          <th>الموظف</th>
          <th>الإيميل</th>
          <th>موقع العمل</th>
          <th>آخر دخول ناجح</th>
          <th>IP</th>
          <th>User-Agent</th>
        </tr>
      </thead>
      <tbody>
        {% for u in rows %}
          <tr>
            <td>{{ loop.index }}</td>
            <td>{{ u.full_name or u.name or u.email }}</td>
            <td>{{ u.email }}</td>
            <td>
              {% set ef = u.employee_file %}
              {% if ef and ef.work_location_lookup_id %}
                {% for x in work_locations %}{% if x.id == ef.work_location_lookup_id %}{{ x.name }}{% endif %}{% endfor %}
              {% else %}—{% endif %}
            </td>
            <td>{{ u.last_login_success_at.strftime('%Y-%m-%d %H:%M') if u.last_login_success_at else '—' }}</td>
            <td>{{ u.last_login_success_ip or '—' }}</td>
            <td class="text-muted small">{{ u.last_login_success_ua or '—' }}</td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>
{% endblock %}
