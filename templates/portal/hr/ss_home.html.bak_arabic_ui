{% extends "portal/hr/base.html" %}
{% block title %}الطلبات الداخلية{% endblock %}

{% block hr_content %}
<div class="p-4 bg-white rounded-3 shadow-sm mb-3">
  <div class="d-flex justify-content-between align-items-start flex-wrap gap-2">
    <div>
      <h4 class="mb-1"><i class="bi bi-ui-checks"></i> الطلبات الداخلية (Self-Service)</h4>
      <div class="text-muted">طلبات شهادة/تعريف/إفادة دوام، تحديث بيانات، رفع مستندات. سير عمل خفيف داخل HR.</div>
    </div>
    <div class="d-flex gap-2">
      <a class="btn btn-outline-secondary" href="{{ url_for('portal.hr_ss_my_requests') }}"><i class="bi bi-list-ul"></i> طلباتي</a>
      {% if current_user.has_perm('HR_SS_APPROVE') %}
        <a class="btn btn-outline-primary" href="{{ url_for('portal.hr_ss_approvals') }}"><i class="bi bi-check2-circle"></i> الموافقات</a>
      {% endif %}
    </div>
  </div>
</div>

<div class="row g-3 mb-3">
  {% for d in defs %}
  <div class="col-12 col-md-6 col-lg-4">
    <div class="p-3 bg-white rounded-3 shadow-sm h-100">
      <div class="fw-bold mb-1">{{ d.name_ar }}</div>
      <div class="text-muted small mb-3">{{ d.code }}</div>
      {% if current_user.has_perm('HR_SS_CREATE') %}
        <a class="btn btn-primary w-100" href="{{ url_for('portal.hr_ss_new_request', type_code=d.code) }}">إنشاء طلب</a>
      {% else %}
        <button class="btn btn-secondary w-100" disabled>لا تملك صلاحية الإنشاء</button>
      {% endif %}
    </div>
  </div>
  {% endfor %}
</div>

<div class="p-3 bg-white rounded-3 shadow-sm">
  <div class="d-flex justify-content-between align-items-center mb-2">
    <div class="fw-bold"><i class="bi bi-clock-history"></i> أحدث طلباتي</div>
    <a class="btn btn-sm btn-outline-secondary" href="{{ url_for('portal.hr_ss_my_requests') }}">عرض الكل</a>
  </div>

  {% if recent %}
    <div class="table-responsive">
      <table class="table table-sm align-middle">
        <thead>
          <tr>
            <th>{{ portal_sort_link('ID', 'id') }}</th>
            <th>{{ portal_sort_link('النوع', 'type_code') }}</th>
            <th>{{ portal_sort_link('الحالة', 'status') }}</th>
            <th>{{ portal_sort_link('تاريخ الإنشاء', 'created_at') }}</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          {% for r in recent %}
          <tr>
            <td>{{ r.id }}</td>
            <td>{{ r.type_code }}</td>
            <td>
              <span class="badge text-bg-{% if r.status in ['APPROVED'] %}success{% elif r.status in ['REJECTED'] %}danger{% elif r.status in ['RETURNED'] %}warning{% elif r.status in ['IN_REVIEW'] %}primary{% else %}secondary{% endif %}">
                {{ r.status }}
              </span>
            </td>
            <td class="text-muted small">{{ r.created_at.strftime('%Y-%m-%d %H:%M') if r.created_at else '' }}</td>
            <td class="text-end">
              <a class="btn btn-sm btn-outline-primary" href="{{ url_for('portal.hr_ss_view_request', req_id=r.id) }}">فتح</a>
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  {% else %}
    <div class="text-muted">لا يوجد طلبات بعد.</div>
  {% endif %}
</div>
{% endblock %}
