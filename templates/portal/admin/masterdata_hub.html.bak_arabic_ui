{% extends "portal/layout.html" %}
{% block title %}ثوابت النظام{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3 flex-wrap gap-2">
  <div>
    <h4 class="mb-1"><i class="bi bi-sliders"></i> ثوابت النظام</h4>
    <div class="text-muted small">مركز موحّد للوصول إلى ثوابت وإعدادات كل جزئية (ستظهر لك فقط الأقسام التي تملك صلاحيتها).</div>
  </div>
  <a class="btn btn-sm btn-outline-secondary" href="{{ url_for('portal.portal_admin_dashboard') }}">
    <i class="bi bi-grid"></i> لوحة الإدارة
  </a>
</div>

{% for s in sections %}
  <div class="card shadow-sm mb-3">
    <div class="card-body">
      <div class="d-flex justify-content-between align-items-start flex-wrap gap-2">
        <div>
          <div class="fw-bold">{{ s.title }}</div>
          <div class="text-muted small">{{ s.desc }}</div>
        </div>
      </div>

	      <div class="row g-3 mt-1">
	        {# NOTE: "items" is a built-in dict method, so access key explicitly when s is a dict #}
	        {% set _items = (s.get('items', []) if s is mapping else s.items) %}
	        {% for it in _items %}
          <div class="col-12 col-md-6 col-lg-4">
	            <a class="text-decoration-none" href="{{ it['url'] if it is mapping else it.url }}">
              <div class="border rounded-3 p-3 h-100">
                <div class="d-flex align-items-center gap-2">
	                  <i class="bi {{ it['icon'] if it is mapping else it.icon }} fs-4"></i>
                  <div>
	                    <div class="fw-semibold">{{ it['title'] if it is mapping else it.title }}</div>
	                    <div class="text-muted small">{{ (it.get('desc') if it is mapping else it.desc) }}</div>
                  </div>
                </div>
              </div>
            </a>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
{% else %}
  <div class="alert alert-info">لا توجد ثوابت متاحة لك ضمن صلاحياتك الحالية.</div>
{% endfor %}

<div class="alert alert-light border mt-3">
  <div class="fw-semibold mb-1">ملاحظات سريعة</div>
  <ul class="mb-0 small">
    <li><strong>الصادر/الوارد:</strong> إدخال <em>التصنيفات</em> و<em>الجهات المرسلة/المستلمة</em> من قسم <strong>ثوابت الصادر/الوارد</strong>.</li>
    <li><strong>وثائق HR:</strong> إدارة <em>تصنيفات وثائق HR</em> من قسم <strong>تصنيفات وثائق HR</strong> (قابلة للإدارة). ثم تُستخدم عند إنشاء الوثائق.</li>
  </ul>
</div>

{% endblock %}
